name: Evaluation Service CI

on:
  push:
    branches: [ main ]
    paths:
      - 'src/evaluation-service/**'
  pull_request:
    branches: [ main ]
    paths:
      - 'src/evaluation-service/**'

jobs:
  call-go:
    uses: ./.github/workflows/reusable-go-ci.yml
    with:
      service_path: 'src/evaluation-service'
      service_name: 'evaluation-service'
      os: 'ubuntu-latest'
      go-version: '1.21'
    secrets: inherit

  call-docker:
    needs: call-go
    uses: ./.github/workflows/reusable-docker.yml
    with:
      service_path: 'src/evaluation-service'
      image-name: 'evaluation-service'
    secrets: inherit